<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    android:id="@+id/grid_item_container"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/grid_card_view_margin"
    android:clickable="true"
    android:foreground="@drawable/grid_item_selector"
    card_view:cardCornerRadius="@dimen/grid_card_view_corner_radius"
    card_view:cardElevation="@dimen/grid_card_view_elevation"
    card_view:contentPadding="@dimen/grid_card_view_padding">

    <ImageView
        android:id="@+id/grid_item_movie_poster_image"
        android:layout_width="match_parent"
        android:layout_height="@dimen/grid_column_height"
        android:transitionName="shared_poster_image"
        android:scaleType="fitXY"/>

<!--    <TextView
        android:id="@+id/grid_item_movie_name_text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_gravity="bottom"
        android:cacheColorHint="@android:color/transparent"
        android:layout_margin="@dimen/grid_card_textview_margin"
        android:background="@drawable/grid_bottom_nav_selector"
        android:gravity="center"
        android:lines="@integer/movie_lines_nav_grid"
        android:textColor="@color/white"
        android:textSize="@dimen/grid_column_move_name_text_size"
        android:textStyle="bold"/>-->

    <RatingBar
        android:id="@+id/rating"
        style="?android:attr/ratingBarStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:isIndicator="true"/>

</android.support.v7.widget.CardView>
